// Status constants for representing success and failure states.
// This object provides string constants for two common status values.
export const Status = {
    success: "success",  // Represents a successful status.
    failure: "failure"   // Represents a failed status.
};  