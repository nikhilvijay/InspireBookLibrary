//Represents the structure of a book object.
export interface Book {
    imageUrl: String;      // The URL of the book's cover image.
    title: String;         // The title of the book.
    purchaseLink: String;  // The URL where the book can be purchased.
    PublishDate: String;   // The date when the book was published.
}